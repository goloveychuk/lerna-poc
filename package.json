{
  "name": "@lerna-poc/test1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "lerna run build && lerna run watch --parallel",
    "build": "lerna run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.0.0-rc.0",
    "typescript": "^2.9.2"
  }
}
