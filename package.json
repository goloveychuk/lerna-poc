{
  "name": "@lerna-poc/analytics-spa-spa",
  "version": "1.0.14",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "lerna run build && lerna run watch --parallel",
    "build": "lerna run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@lerna-poc/analytics-common": "1.0.12",
    "@lerna-poc/analytics-ui": "1.0.13",
    "lerna": "^3.0.0-rc.0",
    "typescript": "^2.9.2"
  }
}
